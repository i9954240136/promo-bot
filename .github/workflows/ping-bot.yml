name: Ping Bot Every Minute

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot root
        run: |
          curl -X GET https://promo-bot-ex86.onrender.com/ || echo "Ping failed"
