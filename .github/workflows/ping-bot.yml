name: Ping Bot Every Minute

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot
        run: |
          curl http://promo-bot-ex86.onrender.com:10001/ping || echo "Ping failed"
